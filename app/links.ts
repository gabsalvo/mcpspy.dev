/**
 * The npm listing, pinned to the release this site documents.
 * Bump on every publish — a pinned /v/ URL does not follow `latest`.
 */
export const NPM_VERSION = '1.1.1';
export const NPM_URL = `https://www.npmjs.com/package/mcp-spy/v/${NPM_VERSION}`;

export const GITHUB_URL = 'https://github.com/gabsalvo/mcpspy.dev';
